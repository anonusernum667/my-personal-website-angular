import { ShuffleobjectsPipe } from './shuffleobjects.pipe';

describe('ShuffleobjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShuffleobjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
